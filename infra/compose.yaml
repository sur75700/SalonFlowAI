services:
  mongo:
    image: mongodb/mongodb-community-server:latest
    container_name: salonflow-mongo
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data:
